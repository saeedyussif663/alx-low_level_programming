#include "main.h"


/** 
 * __puts- prints a string followed by a new line
 * @str: a pointer to the string
 * Return: Always 0 (success)
 */

void _puts(char *str)
{
int i;

for (i = 0; s[i] != '\0'; i++)
{
_putchar(s[i]);
}
_putchar('\n');
}
