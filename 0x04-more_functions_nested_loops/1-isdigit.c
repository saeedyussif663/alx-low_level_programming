#include "main.h"

/**
  * _isdigit - checks if a given input is a digit or not
  *@c: function arguement
  * Return: An integer value
  */

int _isdigit(int c)
{

if (isdigit(c))
return 1;
else
return 0;

}
