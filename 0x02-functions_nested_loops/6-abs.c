#include "main.h"
/**
 * _abs - fuction to find the absolute value of a number
 * @a: fuction parameter
 * Return: -a or a:
 */

int _abs(int a)
{
if (a < 0)
return (-a);
else if (a >= 0)
{
return (a);
}
return (0);
}
