#include <string.h>
#include "main.h"

/**
 * _strcmp - compare to strings
 * @s1: parameter
 * @s2: parameter
 * Return: interger
 */

int _strcmp(char *s1, char *s2)
{
int i;
i = strcmp(s1, s2);
return (i);
}
