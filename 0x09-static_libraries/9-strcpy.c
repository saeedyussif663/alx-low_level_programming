#include "main.h"
#include <string.h>

/**
 * _strcpy - copies string
 *@dest: resultant of copy
 *@src: item to be copied
 * Return: char;
 */

char *_strcpy(char *dest, char *src)
{
strcpy(dest, src);
return (dest);
}
